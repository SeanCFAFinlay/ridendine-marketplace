# Rebuild Tracker

Local rebuild on branch `rebuild/local`. No remote pushes.

- [x] Phase 1 — Repo hygiene
- [x] Phase 2 — Schema consolidation and RLS alignment
- [x] Phase 3 — Collapse parallel order and dispatch engines
- [x] Phase 4 — Schedulers and notification delivery
- [x] Phase 5 — Centralized auth and RBAC enforcement
- [x] Phase 6 — Stripe end-to-end correctness
- [x] Phase 7 — Shared UI primitives and design tokens
- [x] Phase 8 — Rebuild the ops-admin UI
- [x] Phase 9 — Customer experience polish
- [x] Phase 10 — Chef and driver app polish
- [x] Phase 11 — Local end-to-end Playwright tests
- [x] Phase 12 — Local launch readiness

## Notes
_One-line note per phase. Append, never delete._

- Phase 1 (commit 063d58b): archived 391 audit-style files into `archive/audits/`; markdown count under 80.
- Phase 2 (commit 5ba727f): added `00024_canonical_consolidation.sql` (idempotent ADDs + deprecation comments) and `00025_rls_role_alignment.sql` (`is_platform_staff`/`is_finance_staff`/`is_support_staff` helpers); 33 pgTAP tests; FND-002/003/020 resolved; ledger_entries SELECT tightened to `is_finance_staff` (engine bypass via service role still works).
- Phase 3: deleted `order.orchestrator.ts` (1,589 LOC) and `dispatch.engine.ts` (1,660 LOC); created `OrderCreationService`, `DriverMatchingService`, `OfferManagementService`, `DispatchOrchestrator`; extended `MasterOrderEngine` with FND-017 payment void + ledger writes + opsOverride + getAllowedActions + alias methods; added `updateDeliveryStatus` to `DeliveryEngine`; per-request factory (singleton removed); FND-007 scoring moved into `driver-matching.service.ts`; 46 new tests; FND-004/017/018 regression coverage explicit.
- Phase 4: added `sla-tick` + `expired-offers` cron routes (auth via existing `validateEngineProcessorHeaders` which already accepts `Bearer ${CRON_SECRET}`); created `scripts/local-cron.mjs` runner (60s/30s intervals); added `sms-provider.ts` (Twilio direct-fetch, no SDK) + `sms-templates.ts`; registered Twilio as second NotificationDeliveryProvider; constructed `NotificationTriggers` in factory and exposed as `engine.triggers`; added `CRON_SECRET` / `RESEND_API_KEY` / `TWILIO_*` to `.env.example`. Manual end-to-end email/SMS smoke deferred — credentials not present locally.
- Phase 5: relocated capability list to `packages/types/src/capabilities.ts`; closed route gaps (web cart/addresses/profile/favorites/notifications/reviews/support/upload, chef-admin profile/upload/payouts-setup); deleted `BYPASS_AUTH` (replaced with `ALLOW_DEV_AUTOLOGIN` flag, dev-only); architecture test `scripts/audit/check-api-route-guards.mjs` (`pnpm audit:guards`) — scans 92 routes, allowlists 13 public/auth/webhook/health, finds zero unguarded; extended `platform-api-guards.test.ts` to 309 tests covering 35 capabilities × allowed/denied role matrix; rewrote `docs/AUTH_ROLE_MATRIX.md` with full capability×role table.
- Phase 6: 4 idempotency regression tests (2 checkout, 2 stripe webhook); `TaxConfigService` reads `hst_rate`/`service_fee_percent` from `platform_settings` with 60s in-memory cache + constant defaults on DB error; wired into `OrderCreationService.createOrder` — runtime path now reads tax rates from DB. Chef Connect redirect already used `NEXT_PUBLIC_CHEF_ADMIN_URL || NEXT_PUBLIC_APP_URL` (added 3 baseUrl-resolution unit tests). Reconciliation page reads `stripe_reconciliation` directly with `limit(200)` and renders rows — working as-is. Total Stripe-related tests: 147+. FND-017 regression coverage from Phase 3 remains intact.
- Phase 7: `packages/config/src/tokens.ts` defines the canonical brand/surface/border/text/status palette + radii + shadows. 8 shared UI primitives live in `packages/ui/src/components/`: `auth-layout`, `data-table`, `empty-state`, `kpi-tile`, `modal`, `page-header`, `password-strength`, `status-badge` (joining existing `button`, `card`, `badge`, `input`, etc). `lucide-react` added for nav icons. Ops-admin migrated to semantic tokens (`bg-opsCanvas` / `bg-opsPanel` / `bg-opsPanelElevated`) — zero raw `#0f1923`/`#0d1520`/`#16213e`/`#1a1a2e` hex classes remain. `DEFAULT_MAP_CENTER` lives in `@ridendine/utils/geo`; non-test source no longer hard-codes Hamilton coords. Removed the dev-only `/ui-blueprint/[screen]` demo route that was blocking static generation.
- Phase 8: rewrote `apps/ops-admin/src/components/DashboardLayout.tsx` — 21-flat-item sidebar replaced with 5 collapsible nav groups (Operate / Orders & Deliveries / People / Money / Platform), localStorage persistence, lucide group icons, icon-only `md` sidebar, slide-over mobile drawer, top command bar with QuickCreate menu + GlobalSearch + OpsAlerts bell. Live Board (`/dashboard`) now uses `PageHeader` + 4×`KpiTile` + Engine Pressure mini-strip + 3-column layout (orders pipeline / Leaflet map / drivers). All list pages (chefs, drivers, customers, refunds, payouts, reconciliation, dispatch offer history) use `DataTable` + `EmptyState` + `StatusBadge`. Add-Chef / Add-Driver / Force-Assign use the new `Modal` primitive. All inline `<svg>` nav icons replaced with lucide-react components. Existing 65 ops-admin tests still pass; build 4/4; audit:guards exit 0.
- Phase 9: home page now server-fetches active chef + live menu item counts and uses `<FeaturedChefs limit={3} />` (zero-chefs fallback hero); zero hardcoded chef names remain in `apps/web/src/`. Tracking shows public step bar (Placed → Cooking → On the way → Delivered) + ETA + driver first name only after `picked_up` (no coords leaked). Checkout has tip presets (15/20/25/Custom, default 18), debounced promo validation via new `/api/promos/validate`, ETA badge, sticky mobile Pay bar. `/account/orders` lists past orders with a Reorder action that POSTs items to `/api/cart` and redirects. Command-center subsystem `git mv`d from `apps/web/src/app/internal/` to `apps/ops-admin/src/app/internal/` (Phase 5 audit allowlist updated). Engine, web, ops-admin, chef-admin, driver-app tests all green; build 4/4.
- Phase 10: chef-admin storefront page replaces three dead-end states ("not authenticated", "no chef profile", "error") with actionable `EmptyState`s linking to setup. Dashboard shows an amber Stripe Connect banner when `chef_payout_accounts.stripe_account_id` is null (links to `/dashboard/payouts/setup`). Orders list now wires a toast notification on the existing realtime INSERT subscription (`OrderToast` extracted to its own file to keep `orders-list.tsx` under 400 LOC). Driver-app `DeliveryDetail` wires `RouteMap` (pickup/dropoff pins, current driver position) and gates `arrived_at_pickup → picked_up` behind a photo-capture modal (POSTs to `/api/upload`); dropoff already required photo+signature, both now uploaded before the `delivered` transition. Both apps gain a Notification Preferences settings panel (6 events × email/SMS toggles, persisted to localStorage; DB migration deferred). Driver-app gets a PWA manifest (`public/manifest.webmanifest` + theme-color/apple-mobile-web-app-capable meta tags in layout). Largest file in chef-admin is now 381 LOC (`orders-list.tsx`); all files under the 400-LOC gate.
- Phase 11: 5 Playwright lifecycle specs (`e2e/lifecycle/{customer,chef,driver,ops,smoke}.spec.ts`) — 56 tests total. `customer` covers sign-up → browse → cart → checkout (Stripe test card, skips when STRIPE_SECRET_KEY absent) → tracking → review. `chef`: signup → storefront setup → menu → accept-order → ready → payouts. `driver`: signup → vehicle → online → accept offer → pickup-with-photo → dropoff-with-signature → earnings. `ops`: live board → force-assign → approve chef → refund (Stripe-gated) → payouts → reconciliation. `smoke`: 28 tests = 7 key routes × 4 apps, asserts no 5xx + no console errors. Test harness scripts in root `package.json`: `test:e2e:setup` resets the local DB and seeds; `test:e2e:lifecycle` boots all 4 dev servers via `concurrently` + `wait-on`, then runs the suite. Specs typecheck cleanly. End-to-end run deferred to local execution by the user (`pnpm test:e2e:lifecycle`); seed gaps (pending-approval chef, pending offer for driver, fixture id alignment) documented inline in commit + tracker for follow-up.
- Phase 12 (2026-05-05): local launch readiness. Env matrix in `docs/ENVIRONMENT_VARIABLES.md` expanded to cover Resend, Twilio, Sentry DSN/auth token, Google Maps key (9 vars added). Health endpoints confirmed present and correct in all 4 apps (operationalHealthPayload shape: ok/db/env/stripe/rateLimit/readiness). Sentry config files (client/server/edge × 4 apps = 12 files) confirmed — all read `NEXT_PUBLIC_SENTRY_DSN`, no-op when unset, `tracesSampleRate: 0.1` in prod. Maintenance mode: confirmed ops-admin toggle writes to `platform_settings.setting_value.maintenance_mode`; added web middleware maintenance check (30s in-memory cache, fail-open, bypasses `/api/`, `/_next/`) + `/maintenance` page. Legal pages exist but flagged LAUNCH BLOCKER — auto-generated placeholder text not reviewed by counsel; TODO banners added. Seed gaps addressed: added `pending.chef@ridendine.ca` auth user + `a1a1a1a1` chef_profile (`pending_approval`) for ops approve-chef test; added `b2b2b2b2` delivery (pending, no driver) for driver offer-flow test. `e2e/fixtures/test-data.ts` updated to canonical seed UUIDs (`dddddddd` storefront, `item-eby-01` menu item) + new `seed.pendingChefProfileId` / `seed.pendingDeliveryId` constants. `docs/LAUNCH_CHECKLIST.md` expanded: T9–T16 (Stripe live keys, DNS, SSL, E2E gate, ENGINE_PROCESSOR_TOKEN, Upstash, notification-prefs migration, Vercel Cron); L1–L2 marked BLOCKER; L6–L9 added (first real chef/driver, payment agreement, support contact).

## Next

All 12 phases of the local rebuild are complete. Before production launch:

1. **Run `pnpm test:e2e:lifecycle`** end-to-end against a `supabase db reset` local stack — this is the first full green run; seed gaps are now resolved.
2. **Address `docs/LAUNCH_CHECKLIST.md`** items in order: BLOCKER items (L1 legal, L2 privacy, T9 Stripe live keys, T10 DNS, T11 SSL) must be resolved before any production traffic.
3. **Replace per-instance rate limiter with Upstash Redis** (Checklist T14) before scaling beyond a single Vercel instance — the current in-memory bucket does not share state across instances.
4. **Migrate notification preferences from localStorage to DB** (Checklist T15): create and apply `supabase/migrations/00026_notification_preferences.sql`, update the driver-app and chef-admin settings panels to read/write via API (Phase 10 deferral).
5. **Configure Vercel Cron** (Checklist T16): add `vercel.json` cron entries for `sla-tick` (every 60s) and `expired-offers` (every 30s) to replace the local `scripts/local-cron.mjs` runner in production.
