{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo build --filter=@ridendine/driver-app",
  "outputDirectory": "apps/driver-app/.next",
  "env": {
    "BYPASS_AUTH": "true"
  }
}
