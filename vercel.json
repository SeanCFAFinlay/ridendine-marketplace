{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo build --filter=@ridendine/ops-admin",
  "outputDirectory": "apps/ops-admin/.next",
  "env": {
    "BYPASS_AUTH": "true"
  }
}
