{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm turbo build --filter=@ridendine/web",
  "outputDirectory": "apps/web/.next",
  "env": {
    "BYPASS_AUTH": "true"
  }
}
